{
  "name": "explode.js",
  "version": "1.0.0",
  "description": "Creates particle explosions on a HTML5 canvas element.",
  "main": "index.js",
  "author": "Steve Holgado",
  "license": "MIT",
  "homepage": "",
  "repository": {
    "type": "git",
    "url": ""
  },
  "scripts": {
    "clean": "rimraf dist",
    "create": "mkdir dist",
    "js": "browserify src/index.js --transform [ babelify --presets [ es2015 ] ] | uglifyjs --compress --mangle > dist/explode.min.js",
    "dist": "npm run clean && npm run create && npm run js"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.3.0",
    "rimraf": "^2.6.1",
    "uglify-js": "^2.8.22"
  }
}
